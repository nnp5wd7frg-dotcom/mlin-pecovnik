'use client';
import Link from 'next/link';import { Menu, X } from 'lucide-react';import { useState } from 'react';import { nav } from '@/lib/data';
export default function Header(){const[open,setOpen]=useState(false);return <header className="fixed inset-x-0 top-0 z-50 bg-flour/85 backdrop-blur-xl border-b border-bran/10"><div className="container flex h-20 items-center justify-between"><Link href="/" className="font-display text-2xl font-black tracking-tight">Mlin <span className="text-wheat">Pečovnik</span></Link><nav className="hidden lg:flex items-center gap-8">{nav.map(n=><Link className="nav-link text-sm font-bold" key={n.href} href={n.href}>{n.label}</Link>)}</nav><Link href="/kontakt" className="hidden lg:inline-flex btn btn-primary">Pošlji povpraševanje</Link><button className="lg:hidden" onClick={()=>setOpen(!open)} aria-label="Meni">{open?<X/>:<Menu/>}</button></div>{open&&<div className="lg:hidden border-t border-bran/10 bg-flour px-5 pb-6">{nav.map(n=><Link onClick={()=>setOpen(false)} className="block py-4 font-bold" key={n.href} href={n.href}>{n.label}</Link>)}<Link onClick={()=>setOpen(false)} href="/kontakt" className="btn btn-primary w-full justify-center">Kontakt</Link></div>}</header>}
